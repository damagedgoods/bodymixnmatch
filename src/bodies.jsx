var doc = app.activeDocument;var groups = doc.groupItems;var heads = new Array();var bodies = new Array();var feet = new Array(); // Creo los arrays de componentes for (var i=0; i<groups.length; i++) {        var group= groups[i];          if (group.name.indexOf("head") > -1) {            heads.push(group);          } else if (group.name.indexOf("body") > -1) {            bodies.push(group);         } else if (group.name.indexOf("feet") > -1) {              feet.push(group);                      }    }//$.writeln(heads.length+" "+bodies.length+" "+feet.length);// Creo los bichosvar numBichos = 40;var colwidth = 4;for (var i=0; i<numBichos; i++) {        var row= Math.floor(i / colwidth);    var col = i % colwidth;        var newHead = heads[Math.floor(Math.random()*heads.length)].duplicate();    newHead.top = -50-300*row;    newHead.left = 50+250*col;       var newBody= bodies[Math.floor(Math.random()*bodies.length)].duplicate();    newBody.top = -50-90 - 300*row;    newBody.left = 50+250*col;    var newFeet= feet[Math.floor(Math.random()*feet.length)].duplicate();    newFeet.top = -50-180 -300*row;    newFeet.left = 50+250*col;}